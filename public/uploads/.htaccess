# Prevent PHP execution in this directory
<FilesMatch "\.(php|php5|php7|php8|phtml|phar|pl|py|cgi|asp|aspx)$">
    Order Deny,Allow
    Deny from all
</FilesMatch>

# Only allow specific media types (Optional, stricter)
# <FilesMatch "\.(jpg|jpeg|png|gif|pdf)$">
#    Order Allow,Deny
#    Allow from all
# </FilesMatch>
